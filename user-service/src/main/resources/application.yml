server:
  port: 9001

spring:
  application:
    name: USER-SERVICE
  h2.console.enabled: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9010/eureka/
  instance:
    hostname: localhost